# 本目录放本机密钥文件（0600），如各账号 JWT。已被 .gitignore 忽略，永不提交。
# 用法：echo -n "你的JWT" > config/credentials/zcode-jwt && chmod 600 config/credentials/zcode-jwt
# 配置里引用：credential: { api_key_file: "config/credentials/zcode-jwt" }
